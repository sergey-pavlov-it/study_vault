# **Конспект лекций C#**

## 🧭 **Навигация**



## 📚 **Словарь**

* **IL** — Intermediate Language (промежуточный язык).
* **CLR** — Common Language Runtime (общеязыковая среда выполнения).
* **JIT** — Just-In-Time compiler (компилятор «на лету»).
* **GC** — Garbage Collector (сборщик мусора).

---

## 🤖 **Промт для оформления лекций**

````
Модуль ## эмодзи 🔥 жирный шрифт  
Заголовки ### эмодзи ⚙️ жирный шрифт
Подзаголовок #### эмодзи 📂 жирный шрифт
Пример кода #### эмодзи 💻 жирный шрифт
Итог для модуля #### эмодзи ✅ жирный шрифт
Код/команды должны выделяться в блоках: ```bash команда ```
````

---

### ⚙️ **Порядок элементов внутри класса**

| Позиция | Раздел                       | Пример                                 |
| ------- | ---------------------------- | -------------------------------------- |
| 1       | `using`                      | `using System;`                        |
| 2       | `namespace`                  | `namespace MyProject {}`               |
| 3       | Класс / структура / record   | `public class Person {}`               |
| 4       | Поля                         | `private int _age;`                    |
| 5       | Константы и статические поля | `public const int Max = 10;`           |
| 6       | Свойства                     | `public string Name { get; set; }`     |
| 7       | Индексаторы                  | `public int this[int i] { get; set; }` |
| 8       | Конструкторы                 | `public Person(...) {}`                |
| 9       | Методы                       | `public void Print() {}`               |
| 10       | Статические методы          | `public static void Reset() {}`        |

#### 💻 **Код примера**

```csharp
using System;           // Пространства имён (директивы using)
using System.Collections.Generic;

namespace MyProject      // Пространство имён
{
    public class Person  // Класс
    {
        // 1. Поля (fields)
        private string _name;
        private int _age;
        private List<string> _achievements; // внутренний список достижений

        // 2. Константы и статические поля
        public const int MaxAge = 120;
        private static int _count;

        // 3. Свойства (properties)
        public string Name
        {
            get => _name;
            set => _name = value;
        }

        public int Age
        {
            get => _age;
            set
            {
                if (value < 0) throw new Exception("Возраст не может быть отрицательным");
                _age = value;
            }
        }

        // 4. Индексатор (indexer)
        public string this[int index]
        {
            get => _achievements[index];
            set => _achievements[index] = value;
        }

        // 5. Конструкторы
        public Person(string name, int age)
        {
            Name = name;
            Age = age;
            _achievements = new List<string>();
            _count++;
        }

        // 6. Методы
        public void AddAchievement(string title)
        {
            _achievements.Add(title);
        }

        public void ShowAchievements()
        {
            Console.WriteLine($"{Name} — достижения:");
            foreach (var item in _achievements)
                Console.WriteLine($" - {item}");
        }

        public void SayHello()
        {
            Console.WriteLine($"Привет, меня зовут {Name}, мне {Age} лет.");
        }

        // 7. Статические методы
        public static int GetCount()
        {
            return _count;
        }
    }
}
```

---

### ⚙️ **Принятые обозначения и стиль кода**

| Элемент                  | Стиль записи                  | Пример                            |
| ------------------------ | ----------------------------- | --------------------------------- |
| **Пространства имён**    | PascalCase                    | `namespace GameEngine`            |
| **Классы, структуры**    | PascalCase                    | `public class Player`             |
| **Методы**               | PascalCase                    | `public void Move()`              |
| **Свойства**             | PascalCase                    | `public int Health { get; set; }` |
| **Поля (приватные)**     | _camelCase (с подчёркиванием) | `private int _speed;`             |
| **Локальные переменные** | camelCase                     | `int counter = 0;`                |
| **Параметры метода**     | camelCase                     | `void Run(int speed)`             |
| **Константы**            | SCREAMING_CASE                | `const int MAX_HP = 100;`         |
| **Статические поля**     | _camelCase                    | `private static int _count;`      |

---
---

## 🔥 **Модель выполнения программы на C#**

### ⚙️ **Типы языков программирования**

1. **Компилируемые** — весь код переводится в машинный (exe/out). Примеры: C++, Go, Rust.
2. **Интерпретируемые** — выполняются построчно через интерпретатор. Примеры: Python, JS, PHP.
3. **Компилируемо-интерпретируемые** — код → IL/байткод → CLR/VM + JIT. Примеры: C#, Java.

---

### ⚙️ **Выполнение программы**

* Код C# компилируется в **IL**.
* IL выполняется через **CLR**.
* **JIT** переводит IL в машинный код при запуске.
* Повторные вызовы быстрее (код кэшируется).

#### 📂 **CLR**

* Виртуальная машина .NET.
* Управляет памятью, библиотеками, выполнением.
* Все .NET языки совместимы.

#### 📂 **.NET эволюция**

* Было: .NET Framework (Windows).
* Потом: .NET Core (кроссплатформенный).
* Сейчас: .NET 5+ (единая платформа для любых приложений).

---

### ⚙️ **Память в C#**

#### 📂 **Стек (Stack)**

* Малый объём (\~1 МБ).
* Очень быстрый.
* Автоматическое освобождение.
* Хранит локальные переменные, параметры, вызовы методов.

#### 📂 **Куча (Heap)**

* Большой объём.
* Медленнее.
* Управляется **GC**.
* Хранит объекты и массивы.

#### ✅ **Кратко главное**

| Область  | Характеристика                            |
| -------- | ----------------------------------------- |
| **Стек** | Малый объём, быстрый доступ, авто-очистка |
| **Куча** | Большой объём, медленнее, очистка GC      |

---
---

## 🔥 **Типы данных**

### ⚙️ **Ссылочные и значимые типы данных**

#### 📂 **Переменная в памяти**

* **Имя**
* **Тип данных** (размер памяти)
* **Значение**

```csharp
int a = 25; // имя: a, тип: int, значение: 25
```

#### 📂 **Value vs Reference types**

* **Value types** (хранят данные напрямую, обычно в стеке): `int`, `bool`, `double`, `char`, `struct`.
* **Reference types** (в стеке ссылка, данные в куче): `string`, массивы, списки, классы.

#### 📂 **Хранение значений переменных**

```csharp
int n = 10;
string s = "Iron";
int[] a = new int[n];
```

* В стеке: `n = 10`, `s → ссылка`, `a → ссылка`.
* В куче: строка `"Iron"`, массив `int[n]`.

---

### ⚙️ **Копирование и сравнение значений**

#### 📂 **Копирование**

* **Value types** → создаётся копия значения.

```csharp
int a = 2;
int b = a; // b = 2, независим от a
```

* **Reference types** → копируется ссылка.

```csharp
int[] c = {1};
int[] d = c;
c[0] = 5;
Console.WriteLine(d[0]); // 5
```

#### 📂 **Сравнение**

* **Value types** → сравнение по значению.

```csharp
int a = 5, b = 5;
Console.WriteLine(a == b); // True
```

* **Reference types** → сравнение по ссылке.

```csharp
int[] x = {1}, y = {1}, z = x;
Console.WriteLine(x == y); // False
Console.WriteLine(x == z); // True
```

---

### ⚙️ **Передача параметров в функцию**

* При запуске программы стартует `Main` — точка входа.
* Каждой функции в стеке выделяется отдельный **фрейм** (frame).
* В фрейм заносятся: **параметры функции** и **локальные переменные**.
* После завершения функции её фрейм удаляется (указатель стека сдвигается).
* Для ссылочных типов копируется **ссылка**, для значимых — само **значение**.

---

#### 💻 **Пример**

```csharp
using System;

namespace CSharpEssentials
{
    class Program
    {
        static void Print(int[] values)
        {
            int length = values.Length;
            for (int i = 0; i < length; i++)
            {
                Console.Write(values[i] + " ");
            }
        }

        static void Change(int[] array)
        {
            int len = array.Length;
            for (int i = 0; i < len; i++)
            {
                array[i] = array[i] * 2;
            }
        }

        static void Main()
        {
            int[] a = new int[] { 1, 4, 5, 7 };
            Print(a);
            Change(a);
            Print(a);
            Console.WriteLine("Конец Main...");
        }
    }
}
```

---

#### 📂 **Работа стека**

1. При вызове `Print(a)` создаётся новый **фрейм** `Print`:

   * В него копируется параметр `values` (ссылка на массив `a`).
   * Добавляются локальные переменные функции (`length`).

2. При вызове `Change(a)` создаётся новый фрейм `Change`:

   * Параметр `array` указывает на тот же массив в куче.
   * Массив изменяется по ссылке → данные меняются и в `a`.

3. После выхода из функции фрейм удаляется, память стека освобождается **автоматически**.

---

#### 📂 **Выводы**

* **Значимые типы**:

  * В параметры передаётся **копия** значения.
  * Переменная живёт до конца фрейма стека.
* **Ссылочные типы**:

  * В параметры передаётся **ссылка** на объект в куче.
  * Переменные живут, пока объект не удалит **GC**.
* **Указатель стека**: всегда указывает на свободное место. Когда функция завершается, память под её фрейм автоматически освобождается.

---

#### ✅ **Кратко главное**

* В C# все типы делятся на **значимые (Value types)** и **ссылочные (Reference types)**.
* **Value types** хранят значение **в стеке**, а **Reference types** — **ссылку в стеке**, данные — **в куче**.
* При копировании **значимого типа** создаётся **независимая копия данных**.
* При копировании **ссылочного типа** копируется **ссылка на тот же объект**, поэтому изменения видны везде.
* **Value types** сравниваются **по значению**, **Reference types** — **по ссылке**.
* Каждый вызов функции создаёт **новый фрейм стека**, где хранятся параметры и локальные переменные.
* После завершения функции фрейм **автоматически удаляется** — стек освобождается.
* При передаче параметров в метод **значимые типы копируются**, **ссылочные передают ссылку**.
* **GC (Garbage Collector)** следит за памятью в куче и очищает неиспользуемые объекты.
* Итог: стек — для временных данных и управления вызовами, куча — для хранения объектов, а разница между **value** и **reference** лежит в том, **что именно копируется** — данные или ссылка.

---
---

## 🔥 **Функции**

### ⚙️ **ref**

#### 📂 **Суть**

По умолчанию значимые типы (например, `int`) передаются по **значению** — метод получает копию, а не оригинал. Чтобы изменить исходную переменную, используется `ref`.

#### 💻 **Пример**

```csharp
static void Increment(ref int number)
{
    number++;
    Console.WriteLine($"Внутри метода: {number}");
}

static void Main()
{
    int age = 21;
    Increment(ref age);
    Console.WriteLine($"После вызова: {age}"); // 22
}
```

> `ref` указывается и в объявлении метода, и при вызове. Переменная должна быть инициализирована заранее.

**Кратко:** `ref` передаёт ссылку на ту же область памяти, поэтому изменения сохраняются.

---

#### 📂 **ref для ссылочных типов**

```csharp
static void Reset(ref int[] arr)
{
    arr = new int[5]; // меняем ссылку
}

int[] nums = { 1, 2, 3 };
Reset(ref nums);
```

> Без `ref` ссылка бы не изменилась, а создалась бы новая переменная внутри метода.

---

### ⚙️ **out**

#### 📂 **Суть**

`out` похож на `ref`, но используется для **возврата значений** из метода. Переменная не обязана быть инициализирована до вызова.

#### 💻 **Пример: TryParse**

```csharp
if (int.TryParse("2024", out int value))
    Console.WriteLine(value); // 2024
else
    Console.WriteLine("Ошибка");
```

> При `TryParse` значение записывается в `out`-переменную только при успехе.

---

#### 📂 **Несколько значений из метода**

```csharp
static void GetRectangle(int w, int h, out int area, out int perim)
{
    area = w * h;
    perim = (w + h) * 2;
}

GetRectangle(2, 3, out var a, out var p);
Console.WriteLine($"Площадь: {a}, Периметр: {p}");
```

> Все `out`‑параметры должны получить значение внутри метода.

---

#### 📂 **out в ссылочных типах**

```csharp
var dict = new Dictionary<int, string> {{1, "One"}, {2, "Two"}};
if (dict.TryGetValue(1, out string value))
    Console.WriteLine(value); // One
```

> `TryGetValue` возвращает `false`, если ключ не найден, и присваивает `null` переменной `value`.

**Кратко:** `out` — способ вернуть одно или несколько значений без исключений.

---

### ⚙️ **Необязательные и именованные параметры**

#### 📂 **Необязательные параметры**

Параметры могут иметь значение по умолчанию. Если аргумент не передан — используется это значение.

```csharp
static void Info(string name, double price, string category = "Unknown")
{
    Console.WriteLine($"{name}: ${price} ({category})");
}

Info("Smartphone", 799.99);
Info("Headphones", 99.9, "Audio");
```

> После необязательных параметров все остальные тоже должны быть необязательными.

---

#### 📂 **Именованные параметры**

Позволяют вызывать метод, указывая имена аргументов, чтобы не путать порядок.

```csharp
static void Info(string name, double price, string company, string category = "Unknown")
{
    Console.WriteLine($"{name}: ${price} / {company} / {category}");
}

Info("Phone", 799.9, company: "TechCorp", category: "Gadget");
Info(price: 99.9, name: "Mouse", company: "iPear");
```

**Кратко:** значения по умолчанию упрощают вызовы, имена повышают читаемость.

---

### ⚙️ **params**

#### 📂 **Суть**

`params` позволяет передавать в метод любое количество аргументов одного типа.

#### 💻 **Пример**

```csharp
static void Sum(params int[] nums)
{
    int result = 0;
    foreach (var n in nums) result += n;
    Console.WriteLine(result);
}

Sum(1, 2, 3); // 6
Sum();         // 0
```

> Параметр `params` должен быть **одним и последним** в списке.

---

#### 📂 **Комбинация с другими параметрами**

```csharp
static void Sum(string label, params int[] nums)
{
    int total = 0;
    foreach (var n in nums) total += n;
    Console.WriteLine($"{label}{total}");
}

Sum("Сумма: ", 2, 4, 6); // Сумма: 12
```

> После `params` нельзя добавлять другие параметры. Без `params` метод требует явного массива при вызове.

---

#### ✅ **Кратко главное**

| Ключевое слово | Назначение                                     |
| -------------- | ---------------------------------------------- |
| `ref`          | Передает ссылку на переменную                  |
| `out`          | Возвращает значение из метода                  |
| `=`            | Делает параметр необязательным                 |
| `name:`        | Указывает аргумент по имени                    |
| `params`       | Передает любое количество значений одного типа |

---
---

## 🔥 **Строки (String). Часть 1**

### ⚙️ **Общее представление**

Строки — **ссылочный тип данных**, описаны в `string` и хранятся в **куче**.

```csharp
string s = string.Empty; // пустая строка, не null
```

*`Length` — количество символов; `s[i]` — доступ к символу по индексу:*

```csharp
string s = "just a string";
Console.WriteLine(s.Length); // 13
Console.WriteLine(s[0]);     // j
```

---

### ⚙️ **Неизменяемость строк**

Строки **неизменяемы**: изменить символ напрямую нельзя, создаётся новая строка.

```csharp
string s = "just a string";
s += "!";
Console.WriteLine(s); // just a string!
```

> Каждая операция создаёт новый объект в памяти.

---

### ⚙️ **Символы (char)**

Символ — это элемент строки, записывается в одинарных кавычках `' '`.
Каждый символ имеет числовой код (ASCII, Unicode).

```csharp
string s = "Josef";
char c = 'o';
Console.WriteLine(s[2]); // s
```

---

### ⚙️ **String и массив символов**

`string` и `char[]` — последовательности символов, но ведут себя по-разному.

#### 💻 **Изменяемость**

```csharp
string s = "abc";
// s[2] = 'w'; // ошибка
s = "abw";
```

```csharp
char[] arr = { 'a', 'b', 'c' };
arr[2] = 'w';
Console.WriteLine(arr[2]); // w
```

> `string` — неизменяем, `char[]` — изменяем.

---

#### 💻 **Добавление символов**

*Массив символов фиксированной длины:*

```csharp
char[] s = { 'a', 'b', 'c' };
// s += 'd'; // ошибка
```

*Строки позволяют добавлять символы:*

```csharp
string s = "abc";
s += 'd';
Console.WriteLine(s); // abcd
```

---

#### ✅ **Кратко главное**

| Тип        | Изменяемость | Длина          | Добавление символов       | Использование                    |
| ---------- | ------------ | -------------- | ------------------------- | -------------------------------- |
| **string** | Неизменяем   | Автоматическая | Через `+=` (новый объект) | Удобен для чтения текста         |
| **char[]** | Изменяем     | Фиксированная  | Нельзя напрямую           | Эффективен при частых изменениях |

---
---

## 🔥 **Строки (String). Часть 2**

### ⚙️ **Проблема `string`**

Строки — **неизменяемы**: любая операция над ними создаёт **новый объект** в памяти.

```csharp
string s = "Iron";
s += "Programmer";
Console.WriteLine(s); // IronProgrammer
```

В памяти: `"Iron"`, `"Programmer"`, `"IronProgrammer"`.
В циклах это приводит к огромному расходу памяти и времени.

```csharp
string s = string.Empty;
for (int i = 0; i < 50000; i++)
    s += "новая запись";
```

Создаётся 50 000 объектов, сложность — **O(n²)**.

---

### ⚙️ **Решение — `StringBuilder`**

`StringBuilder` — **изменяемый** тип, оптимизированный для многократных изменений строк.

```csharp
using System.Text;
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 50000; i++)
    sb.Append("новая запись");
```

Разница в производительности:

* `string` → ~10 000 мс
* `StringBuilder` → ~1 мс

---

### ⚙️ **Как работает**

При создании выделяется буфер (`char[]`) с вместимостью (**Capacity**) — обычно 16 символов.
При переполнении буфер **удваивается**, данные копируются в новый. Это происходит редко, поэтому работа быстрая.

#### 📂 **Свойства**

| Свойство     | Описание                                                 |
| ------------ | -------------------------------------------------------- |
| **Length**   | Сколько символов записано                                |
| **Capacity** | Сколько символов помещается без перераспределения памяти |

Если известно, сколько текста будет, задайте вместимость заранее:

```csharp
StringBuilder sb = new StringBuilder(600000);
```

---

### ⚙️ **Когда что использовать**

| Использовать        | Когда                                                                     |
| ------------------- | ------------------------------------------------------------------------- |
| **`StringBuilder`** | Много циклов, постоянные изменения, неизвестный объём данных              |
| **`string`**        | Небольшие и редкие изменения, работа с методами (`IndexOf`, `StartsWith`) |

---

### ⚙️ **Управляющие последовательности**

Управляющие (escape) последовательности — специальные символы, начинающиеся с `\\`, выполняют действие вместо вывода текста.

| Последовательность | Назначение            | Пример                                       |
| ------------------ | --------------------- | -------------------------------------------- |
| `\\n`              | новая строка          | `Console.Write("A\\nB");`                    |
| `\\t`              | табуляция             | `Console.Write("A\\tB");`                    |
| `\\r`              | возврат каретки       | используется для обновления текста в консоли |
| `\\\"`             | кавычка внутри строки | `\"text\"`                                   |
| `\\\\`             | обратный слэш         | `"c:\\\\file.txt"`                           |

---

### ⚙️ **Буквальные строки (`@`)**

`@` отключает экранирование — всё в строке воспринимается буквально.

```csharp
string path = @"c:\Users\file";
string @default = "ok"; // можно использовать ключевые слова
```

---

### ✅ **Коротко главное**

* `string` — неизменяем, создаёт новые объекты при изменениях.
* `StringBuilder` — изменяем, экономит память и время.
* `Capacity` управляет размером буфера.
* Управляющие последовательности (`\\n`, `\\t`, `\\r`, `\\\"`, `\\\\`) задают спецсимволы.
* Префикс `@` делает строку «буквальной».

---
---

## 🔥 **Инкапсуляция**
### ⚙️ **Свойства и автосвойства**

* Свойства — это «обёртка» над полями для контроля чтения и записи.
* Позволяют скрывать детали реализации и проверять данные перед присвоением.
* Автосвойства — упрощённая форма без явного поля (компилятор создаёт его сам).

---

#### 💻 **Обычное свойство**

```csharp
private int age;

public int Age
{
	get { return age; }
	set
	{
		if (value < 0) throw new Exception("Возраст не может быть отрицательным");
		age = value;
	}
}
```

#### 💻 **Автосвойство**

```csharp
public string Name { get; set; }
```

#### 💻 **Только чтение (private set)**

```csharp
public int Id { get; private set; }
```

---

#### 📂 **Когда использовать**

| Сценарий                           | Что использовать     |
| ---------------------------------- | -------------------- |
| Простое хранение данных            | **Автосвойство**     |
| Нужна проверка или фильтрация      | **Обычное свойство** |
| Значение вычисляется на лету       | **Только get**       |
| Объект не должен меняться извне    | **private set**      |
| Изменение требует сложных действий | **Метод**            |

---

#### 📂 **Когда выносить в методы**

Если при изменении значения нужно:

* работать с несколькими полями;
* выполнять валидацию, расчёты или побочные эффекты;
* запускать действия, а не просто менять значение.

```csharp
public class Account
{
	public decimal Balance { get; private set; }

	public void Deposit(decimal amount)
	{
		if (amount <= 0) throw new Exception("Сумма должна быть положительной");
		Balance += amount;
	}
}
```

*Метод — это действие.
Свойство — это характеристика.*

---

#### ✅ **Кратко главное**

* **Автосвойство** — простое хранение данных.
* **Обычное свойство** — контроль чтения и записи.
* **Метод** — изменение состояния с логикой.
* Если код в сеттере растёт — пора делать метод.
* `private set` защищает объект от внешнего изменения.

---
---

[⬆ Наверх](#-навигация)